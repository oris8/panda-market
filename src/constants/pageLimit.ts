export const BEST_ITEM_LIMIT: { [key in DeviceSizes]: number } = {
  small: 1,
  medium: 2,
  large: 4,
};

export const ITEM_LIMIT: { [key in DeviceSizes]: number } = {
  small: 4,
  medium: 6,
  large: 10,
};

export const ITEM_COMMENT_LIMIT = 2;

export const BEST_POST_LIMIT: { [key in DeviceSizes]: number } = {
  small: 1,
  medium: 2,
  large: 3,
};

export const POST_LIMIT = 10;

export const POST_COMMENT_LIMIT = 2;
